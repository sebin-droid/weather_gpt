FROM python:3.11-slim
WORKDIR /app
COPY backend/ /app/
RUN pip install --no-cache-dir fastapi uvicorn requests python-dotenv deep-translator
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
